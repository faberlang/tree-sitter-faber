#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT"

python3 scripta/generate_grammar.py "$@"
npx --yes tree-sitter-cli@0.24.7 generate

echo "regenerated grammar.js, highlight queries, and parser artifacts"